get:
  summary: "getSubject"
  description: "Request for getting student"
  tags:
    - Subject
  parameters:
      - name: id
        in: path
        description: Subject ID
        required: true
        schema:
          type: integer
          format: int64
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            properties:
              Student:
                type: object
                properties:
                  id:
                    type: integer
                    format: int64
                  name:
                    type: string
                required:
                  - id
                  - name